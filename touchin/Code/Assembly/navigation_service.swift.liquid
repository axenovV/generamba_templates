import LeadKit
import UIKit

extension NavigationService {

    static func {{ module_info.name | prepend:'create' }}ViewController() -> {{ module_info.name }}ViewController {
        let controller = {{ module_info.name }}ViewController(nibName: {{ module_info.name }}ViewController.xibName, bundle: nil)
        let assembly = {{ module_info.name }}Assembly()
        assembly.configure(input: controller)
        return controller
    }

}
