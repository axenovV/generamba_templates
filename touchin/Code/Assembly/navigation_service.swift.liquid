import UIKit

extension NavigationService {

    static func {{ module_info.name | prepend:'create' }}ViewController() -> {{ module_info.name }}ViewController {
        let viewController: {{ module_info.name }}ViewController = UIStoryboard(storyboard: .{{ module_info.name | capitalize }}).instantiateViewController()
        assembly.configure(input: controller)
        return controller
    }

}
